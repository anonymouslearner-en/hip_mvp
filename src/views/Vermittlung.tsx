import { Navbar } from "@/components/layout/Navbar";
import { Page } from "@/components/layout/Page";

export const Vermittlung = () => {
  return (
    <Page header={<Navbar />}>
      <section className="">
        <h2>Vermittlung</h2>
      </section>
    </Page>
  );
};
